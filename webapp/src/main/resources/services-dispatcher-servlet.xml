<beans xmlns="http://www.springframework.org/schema/beans"
   xmlns:context="http://www.springframework.org/schema/context"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
   xmlns:mvc="http://www.springframework.org/schema/mvc"
   xsi:schemaLocation="
		http://www.springframework.org/schema/beans     
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context 
		http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc.xsd">
    
   	<context:component-scan base-package="org.sagebionetworks.bridge.services.controllers" />
	
   	<mvc:annotation-driven />
   	
   	<!-- 
		This serializer (handler mapping) is defined in org.sagebionetworks:services-repository
		and that's a war project. We'd have to pull it apart and copy over the files to get at the 
		same serializer, as part of the build. But as you'd suspect, there's already a JSON serializer 
		that Spring supports, so why. Let's use that for the moment.

	<bean class="org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerExceptionResolver">
		<property name="messageConverters">
			<list>
				<ref bean="objectTypeSerializer" />
			</list>
		</property>
	</bean>
	<bean id="objectTypeSerializer" class="org.sagebionetworks.repo.web.controller.ObjectTypeSerializerImpl" />
	-->
	
   	<!-- Import other business-related bean files here. -->
 
</beans>